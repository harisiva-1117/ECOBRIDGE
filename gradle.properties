# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseParallelGC
# Run independent tasks in parallel (this machine has 8 cores / 12 threads).
org.gradle.parallel=true
# Reuse configuration on subsequent builds for much faster incremental builds.
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn
# Watch the file system for changes instead of re-scanning on every build.
org.gradle.vfs.watch=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
# Use most of the CPU for parallel task execution (i5-12450H: 8 cores / 12 threads).
org.gradle.workers.max=6
# Keep a persistent Kotlin compiler daemon so incremental compilation stays warm.
kotlin.compiler.execution.strategy=daemon
kotlin.daemon.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
# Compile only what changed between builds.
kotlin.incremental=true
kotlin.incremental.useClasspathSnapshot=true
# Allow the google-services plugin to work even if the google-services.json is not present.
googleServices.missing.passthrough=true
